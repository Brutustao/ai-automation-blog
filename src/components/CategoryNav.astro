---
import { CATEGORIES } from "../config";
---

<nav class="mb-8" aria-label="Categories">
  <ul class="flex flex-wrap gap-2">
    <li>
      <a
        href="/"
        class="inline-block rounded-full border border-gray-300 px-4 py-1.5 text-sm font-medium text-gray-700 transition hover:border-gray-900 hover:text-gray-900 dark:border-gray-700 dark:text-gray-300 dark:hover:border-gray-100 dark:hover:text-gray-100"
      >
        All
      </a>
    </li>
    {CATEGORIES.map((cat) => (
      <li key={cat.slug}>
        <a
          href={`/${cat.slug}`}
          class="inline-block rounded-full border border-gray-300 px-4 py-1.5 text-sm font-medium text-gray-700 transition hover:border-gray-900 hover:text-gray-900 dark:border-gray-700 dark:text-gray-300 dark:hover:border-gray-100 dark:hover:text-gray-100"
        >
          {cat.title}
        </a>
      </li>
    ))}
  </ul>
</nav>
